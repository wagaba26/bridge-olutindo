import { JobsBoard } from "@/components/site/jobs-board";

export const metadata = {
  title: "Jobs in Japan | Bridge Olutindo",
  description:
    "Browse Japanese job pathways by category, location, and language level with Bridge Olutindo support.",
};

export default function JobsPage() {
  return <JobsBoard />;
}
